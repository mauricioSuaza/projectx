
<%transc_color = 'panel-headingtwo'%>
<%transc_border = 'panel-secondaryred'%>
<%if transaction.status%>
   <%transc_color = 'panel-heading'%>
   <%transc_border = 'panel-secondary'%>
<%end%>



<div class="transactions-container">
    <div class="panel-transactions <%=transc_border%> container-fluid">
        <div class="panel-heading">
            <h3 class="panel-title" style="font-size: 30px;">Transaction <%= transaction.id %></h3>
            <i class="fa fa-exchange" aria-hidden="true" style="color: rgba(255, 255, 255, 0.54);"></i>
        </div>
        <table class="table table-condensed table-responsive table-user-information">
            <tbody>
            <tr>
                <td class="left-tab" style="text-align:left;">Value:</td>
                <td><%= transaction.value %></td>
            </tr>
            <tr>
                <td class="left-tab" style="text-align:left;">Time remaining:</td>
                <td>23 Hours</td>
            </tr>
            <tr>
                <td class="left-tab" style="text-align:left;">Status:</td>
                <% if transaction.status%>
                    <td>Completed</td>
                <% else  %>
                    <td>Pending </td>
                <% end%>
            </tr>
            <tr>
                <td class="left-tab" style="text-align:left;">Sender name:</td>
                <td><%= User.find(transaction.sender_id).name %></td>
            </tr>
            <tr>
                <td class="left-tab" style="text-align:left;">Sender phone:</td>
                <td><%= User.find(transaction.sender_id).phone %></td>
            </tr>

            <tr>
                <td class="left-tab" style="text-align:left;">Receiver email:</td>
                <td><%= User.find(transaction.receiver_id).email %></td>
            </tr>
            <tr>
                <td class="left-tab" style="text-align:left;">Receiver wallet</td>
                <td><%= User.find(transaction.receiver_id).btc %></td>
            </tr>

            </tbody>
        </table>

        <div class="buttons-container">
          <div>
              <%= button_to chats_path(sender_id: current_user.id, recipient_id: transaction.receiver_id), class: 'btn btn-primary own-btn-primary' do %>
              <i class="fa fa-weixin i-form" aria-hidden="true"></i>
              Message
              <%end%>
          </div>

          <div>
              <div class="">
                  <% if transaction.invoice.url %>
                      <div>
                          <button type="button" class="btn btn-success btn-sm own-btn-form" data-toggle="modal" data-target="#m<%=transaction.id%>"><i class="fa fa-repeat i-form" aria-hidden="true"style="margin:0px 5px;"></i>Reupload receipt</button>
                      </div>
                      <div>
                          <button type="button" class="btn btn-warning btn-sm own-btn-form2" data-toggle="modal" data-target="#img<%=transaction.id%>"><i class="fa fa-file-text-o i-form" aria-hidden="true" style="margin: 0px 20px 0px 0px;"></i>Check invoice</button>
                      </div>
                  <% else %>
                      <button type="button" class="btn btn-success btn-md" data-toggle="modal" data-target="#m<%=transaction.id%>">
                        <i class="fa fa-upload i-form" aria-hidden="true" style="margin: 0px 5px;"></i>Upload receipt</button>
                  <% end  %>
              </div>
          </div>
        </div>
    </div>
</div>

<!-- Upload Image Modal -->
<div id="<%=transaction.id%>" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Upload Screenshot</h4>
            </div>
            <div class ="request-donation-form">
                <%= form_tag("/add_invoice", method: "post", multipart: true) do %>
                    <div class="modal-body">
                        <%= file_field_tag :invoice %>
                        <%= hidden_field_tag :transaction_id, transaction.id %>
                    </div>
                    <div class="modal-footer">
                        <div class="col-md-4 col-md-offset-4">
                            <%= submit_tag("Upload", class: "btn btn-primary") %>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</div>

<% if transaction.invoice.url.present? %>
    <div id="img<%=transaction.id%>" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Upload Screenshot</h4>
                </div>
                <div class ="request-donation-form">
                    <%= image_tag(transaction.invoice.url)%>
                </div>
            </div>
        </div>
    </div>
<%end%>
